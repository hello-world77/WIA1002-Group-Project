## 1. 使用 GPS 的公交路线与时刻表追踪系统

### 标题

**使用 GPS 的公交路线与时刻表追踪系统**

### 描述

本项目开发一个使用 GPS 实时监控公交车的系统，用于管理路线和时刻表，并向乘客提供当前位置、下一站和预计到达时间等信息。该系统可以设计用于校园接驳车、校车或城市公共交通服务。

### 问题陈述

乘客通常不知道公交车在哪里，也不知道公交车何时到达。基于 GPS 的追踪系统可以通过结合实时位置更新与已存储的路线和时刻表数据来解决这一问题。

### 场景

一名学生选择校园接驳车路线和站点。系统检查公交车的 GPS 位置，确定下一站，计算剩余距离，并显示预计到达时间，例如：

> “T818 公交车将在 4 分钟后到达图书馆。”

### 项目团队与范围

适合最多 5 名学生组成小组完成。工作可以划分为：

- 路线设计
- 时刻表处理
- GPS 模拟
- 搜索 / ETA（预计到达时间）计算
- 用户界面 / 测试

### 可使用的数据结构

| 数据结构 | 用途 |
|---|---|
| 图（Graph） | 公交站点和路线 |
| 队列（Queue） | 即将到达的站点 |
| 链表（Linked List） | 有序路线路径 |
| 哈希表（Hash Table） | 快速查找公交车 / 站点 |
| 优先队列（Priority Queue） | 最近到达的公交车 |

---

## 2. 急救医疗调度系统

### 标题

**急救医疗调度系统**

### 简要描述

本项目模拟一个城市救护车调度系统。当急救电话到达时，系统使用**图（Graph）**进行路线规划，分配最近的可用救护车；使用按医疗严重程度排序的**优先队列（Priority Queue）**对呼叫进行优先级处理；当所有救护车都在忙碌时，使用普通**队列（Queue）**排队处理非紧急呼叫。

### 问题陈述

在紧急情况下，调度员手动查找救护车位置和估算路线会浪费关键时间。一个结合基于图的路径查找、基于严重程度的优先级排序，以及对非紧急呼叫进行公平处理的系统，可以减少响应时间并挽救生命。

### 场景

调度员短时间内连续收到三个呼叫：

1. 心脏病发作——严重程度 1，最高——地点 A
2. 轻微车祸——严重程度 3，最低——地点 B
3. 房屋火灾伴烧伤——严重程度 2，中等——地点 C

### 系统行为

- 优先队列会自动将呼叫排序为：**心脏病发作 → 火灾 → 车祸**。
- 系统通过图检查可用救护车，找到距离心脏病发作地点最近的救护车，并使用 **Dijkstra 算法**计算最短路线。
- 如果两辆救护车都处于忙碌状态，火灾呼叫会在优先队列中等待，车祸呼叫会在普通队列中等待。
- 当一辆救护车空闲后，系统会先检查优先队列。如果优先队列为空，则从普通队列中取出下一个呼叫。

### 评分标准——总分：15 分

| 标准 | 优秀（3） | 良好（2） | 合格（1） | 较差（0） | 分数 |
|---|---|---|---|---|---:|
| 图实现与路径查找 | 正确构建图，包括节点、边和权重。Dijkstra 算法能完美计算最短路径 ETA。 | 图可以工作。Dijkstra 算法可以工作，但有小错误。 | 图不完整，或 Dijkstra 仅适用于简单情况。 | 没有图或没有路径查找。 | 3 |
| 按严重程度的优先队列 | 正确的最大堆行为。严重程度 1 总是先于 2 和 3 处理。插入 / 删除为 `O(log n)`。 | 可以工作，但严重程度顺序有一个错误。 | 有优先队列，但不完全正确。 | 没有优先队列，或完全错误。 | 3 |
| 非紧急呼叫的 FIFO 队列 | 正确的先进先出。仅当优先队列为空时才处理非紧急呼叫。能配合动态救护车可用状态工作。 | FIFO 可以工作，但与优先队列的整合有小缺陷。 | 队列可以独立工作，但未整合。 | 没有队列。 | 3 |
| 系统整合与工作流程 | 呼叫流程正确：优先队列 → 分配救护车 → 路线 → 救护车空闲 → 从优先队列或普通队列取下一个呼叫。 | 主要流程可以工作，但有一个步骤出错。 | 部分整合。 | 没有整合。 | 2 |
| 代码质量与模块化 | 代码整洁、注释良好，并为每个数据结构使用单独文件 / 模块。 | 较好，但有一些重复。 | 杂乱但可读。 | 不可读。 | 2 |
| 演示与报告 | 演示清晰，包含测试用例。解释为什么选择图、优先队列和队列。包含复杂度分析。 | 演示可以工作，但缺少复杂度分析。 | 演示或报告很少。 | 没有演示。 | 2 |

### 交付内容

1. **Java 源代码**，包括：
   - 带 Dijkstra 方法的 Graph 类
   - Priority Queue 类，或堆实现
   - Queue 类，或双端队列实现
   - 主模拟循环
2. **报告**——2 到 3 页
3. **演示视频**——5 分钟

---

## 3. 使用双向链表的大数运算

### 项目标题

**使用双向链表的大数运算**

### 描述

本项目重点设计和实现一个系统，使用双向链表数据结构对非常大的整数进行算术运算。由于这些数字可能超过内置数值数据类型的存储能力，因此每一位数字都会单独存储在双向链表的一个节点中。该项目支持加法、减法、乘法和除法等算术运算。

### 目标

通过本项目，学生应理解双向链表如何应用于涉及大数的实际计算问题，同时加强以下知识：

- 链表遍历
- 动态内存管理
- 按位数字运算处理

### 示例 1

#### 输入

```text
m = 123456789123456789123456789123456789123456789123456789
n = 456789123456789123456789123456789123456789123456789
```

#### 输出

```text
addition = 123913578246913578246913578246913578246913578246913578
subtraction = 123000000000000000000000000000000000000000000000000000
multiplication = 563937184884934839205932493526930147847927802168925...3035101981191820046486820281054720515622620750190521
division = 270.27085975512599935528
```

### 示例 2

#### 输入

```text
m = 55
n = 2
```

#### 输出

```text
addition = 57
subtraction = 53
multiplication = 110
division = 27.5
```

### 方法

首先，将大数拆分为单个数字，并存储在双向链表中。

加法和减法按照标准算术方法逐位执行，包括处理进位和借位。这些基本操作随后用于实现乘法和除法。

乘法通过逐位相乘来完成，然后对中间结果进行移位并相加。除法通过识别除数的合适倍数，并逐步从被除数中减去这些倍数来完成。

最后，通过显示函数输出计算结果。

---

## 4. 智能图书馆项目

### 标题

**智能图书馆项目**

### 场景

一个大学图书馆系统。图书存储在使用 **BST（二叉搜索树）**的数据库中，以便通过 ISBN 快速搜索。当学生借书时，该图书会被加入他们的**借阅历史**，并使用**栈（Stack）**存储，这样学生可以优先看到最近的借阅活动。

### 目标

理解不同数据结构如何服务于不同的访问模式：

- **LIFO（后进先出）**用于借阅历史
- **对数级搜索**用于图书搜索

### 个人任务——5 名成员

1. **目录架构师：** 构建一个 BST，用 ISBN 作为索引来存储书名和作者。
2. **借阅历史：** 实现一个 Stack，用于记录已借出的图书，最近借出的图书位于栈顶。
3. **记录查找员：** 在 BST 中实现递归搜索函数，通过 ISBN 查找图书。
4. **ADT 设计者：** 为图书馆系统创建接口，以确保信息隐藏。
5. **管理员逻辑：** 处理借阅和归还流程，包括从目录中移除并推入栈中。

### 功能性控制台界面要求

学生需要交付一个**功能性控制台界面**，允许用户（如图书管理员或学生）在不修改源代码的情况下与系统交互。

该界面必须提供菜单驱动体验，包括：

1. **添加图书：** 输入 ISBN、书名和作者。
2. **搜索图书：** 以 `O(log n)` 效率通过 ISBN 查找详细信息。
3. **借阅图书：** 将记录移动到历史栈中。
4. **查看历史：** 按 LIFO 顺序显示所有已借图书。
5. **退出：** 终止程序。

### 示例代码实现

```java
import java.util.*;

// ==========================================
// 接口与实体
// ==========================================
interface LibraryADT {
    void addBook(int isbn, String title, String author);
    void borrowBook(int isbn);
    void viewLatestHistory();
    void searchBook(int isbn);
}

class Book {
    int isbn;
    String title, author;
    Book left, right;

    public Book(int isbn, String title, String author) {
        this.isbn = isbn;
        this.title = title;
        this.author = author;
    }
}

// ==========================================
// BST 与栈逻辑
// ==========================================
class BookBST {
    private Book root;

    public void insert(int isbn, String t, String a) {
        root = ins(root, isbn, t, a);
    }

    private Book ins(Book r, int i, String t, String a) {
        if (r == null) return new Book(i, t, a);
        if (i < r.isbn) {
            r.left = ins(r.left, i, t, a);
        } else if (i > r.isbn) {
            r.right = ins(r.right, i, t, a);
        }
        return r;
    }

    public Book search(int i) {
        return sea(root, i);
    }

    private Book sea(Book r, int i) {
        if (r == null || r.isbn == i) return r;
        return (i < r.isbn) ? sea(r.left, i) : sea(r.right, i);
    }
}

class BorrowStack {
    private Stack<Book> stack = new Stack<>();

    public void push(Book b) {
        stack.push(b);
    }

    public void show() {
        if (stack.isEmpty()) {
            System.out.println("历史记录为空。");
        } else {
            // 反向迭代，用于先显示最近的记录
            for (int i = stack.size() - 1; i >= 0; i--) {
                Book b = stack.get(i);
                System.out.println("[ISBN: " + b.isbn + "] " + b.title);
            }
        }
    }
}

// ==========================================
// 要求 A：功能性控制台界面
// ==========================================
class SmartLibrary implements LibraryADT {
    private BookBST catalogue = new BookBST();
    private BorrowStack history = new BorrowStack();

    public void addBook(int i, String t, String a) {
        catalogue.insert(i, t, a);
    }

    public void searchBook(int i) {
        Book b = catalogue.search(i);
        if (b != null) {
            System.out.println("找到: " + b.title);
        } else {
            System.out.println("未找到。");
        }
    }

    public void borrowBook(int i) {
        Book b = catalogue.search(i);
        if (b != null) {
            history.push(b);
            System.out.println("已借阅 " + b.title);
        } else {
            System.out.println("目录中没有该图书。");
        }
    }

    public void viewLatestHistory() {
        history.show();
    }

    public void runMenu() {
        Scanner sc = new Scanner(System.in);
        while (true) {
            printMenu();
            System.out.print("选择: ");
            int choice = sc.nextInt();
            if (choice == 5) break;
            handleChoice(choice, sc);
        }
        sc.close();
    }

    private void printMenu() {
        System.out.println("\n--- SmartLibrary 菜单 ---");
        System.out.println("1. 添加图书");
        System.out.println("2. 搜索（BST）");
        System.out.println("3. 借阅（Stack）");
        System.out.println("4. 历史记录");
        System.out.println("5. 退出");
    }

    private void handleChoice(int choice, Scanner sc) {
        switch (choice) {
            case 1:
                System.out.print("输入 ISBN: ");
                int i = sc.nextInt();
                System.out.print("输入书名: ");
                String t = sc.next();
                System.out.print("输入作者: ");
                String a = sc.next();
                addBook(i, t, a);
                break;
            case 2:
                System.out.print("输入要搜索的 ISBN: ");
                searchBook(sc.nextInt());
                break;
            case 3:
                System.out.print("输入要借阅的 ISBN: ");
                borrowBook(sc.nextInt());
                break;
            case 4:
                viewLatestHistory();
                break;
            default:
                System.out.println("无效选项。");
        }
    }
}

public class Main {
    public static void main(String[] args) {
        new SmartLibrary().runMenu();
    }
}
```

### 评分标准

| 标准 | 更新后的重点 | 权重 |
|---|---|---:|
| 界面功能 | 控制台菜单运行时不会崩溃，并能优雅处理无效输入。 | 20% |
| BST 搜索逻辑 | 递归搜索能正确遍历树，并具有 `O(log n)` 复杂度。 | 20% |
| 栈历史记录 | 借阅操作能正确压入栈；历史记录按 LIFO 顺序显示。 | 20% |
| 信息隐藏 | 图书馆系统使用 ADT 接口；内部结构为私有。 | 20% |
| 输入验证 | 系统能处理非整数 ISBN 或空搜索结果等情况。 | 20% |

---

## 5. 智能停车与交通管理系统

### 标题

**智能停车与交通管理系统**

### 任务说明

根据以下案例研究，你需要提出能够实现快速检索的数据结构技术，并解释其高效的原因。

### 描述

在智能停车与交通管理系统中，系统必须高效管理停车位置和车辆记录。你需要设计一个模块，用于动态存储和更新停车位及车辆信息。系统应支持添加、删除和显示记录。请提出合适的数据结构并说明你的选择理由。

系统还必须实时处理车辆进入和离开停车区域的情况。车辆应按到达顺序处理，但系统也应支持在发生错误时撤销最近一次操作。请使用合适的数据结构设计解决方案，并解释其工作方式。

此外，系统必须根据优先级分配停车位，例如最近可用停车位或最短等待时间。你需要设计一种机制，高效选择最佳停车位，并说明你的方法与简单搜索相比如何提升性能。

系统还应引导驾驶员前往最近的可用停车位置。你需要表示停车位置和路线，并实现一种方法来确定最短路径。请简要解释你的方法及其效率。

此外，系统应允许用户快速搜索车辆记录和停车信息。请设计一种支持高效搜索和排序的解决方案，并解释它为什么比基本列表表现更好。

最后，系统应能够快速访问常用数据，例如车辆详细信息和停车状态。

### 评分标准

| 组件 | 提出的数据结构技术 | 标准 | 分数 |
|---|---|---|---:|
| 停车与车辆管理 | 数组 / 链表 | 正确选择数据结构，支持动态数据处理、添加 / 删除 / 显示，并说明复杂度。 | 15 |
| 进出处理 | 队列 + 栈 | 正确的 FIFO 处理，以及使用 LIFO 的撤销机制；逻辑可运行并有合理解释。 | 15 |
| 停车位分配 | 优先队列 / 最小堆 | 高效选择停车位，优先级设置正确，并与线性搜索进行比较。 | 15 |
| 路线导航 | 图 + Dijkstra 算法 | 正确建立图模型，实现最短路径，并解释效率。 | 20 |
| 搜索系统 | BST / AVL 树 | 高效搜索和排序，操作正确，并解释 `O(log n)`。 | 15 |
| 快速数据检索 | 哈希表 / HashMap | 高效查找（`O(1)`），正确使用键值对，并进行解释。 | 10 |
| 系统整合 | 所有数据结构结合 | 各模块整合后能作为一个系统运行。 | 5 |
| 展示与报告 | 图示与解释 | 报告清晰、结构合理，并包含相关数据结构图示。 | 5 |
| **总分** |  |  | **100** |

---

## 6. 智能外卖配送与订单管理系统

### 标题

**智能外卖配送与订单管理系统**

### 任务说明

根据以下案例研究，你需要提出能够实现快速检索的数据结构技术，并解释其高效的原因。

### 描述

在智能外卖配送与订单管理系统中，GoodTech 公司需要一种更好的方式来管理用户、餐厅、订单和配送服务。作为一个团队，你们需要使用合适的数据结构设计并实现一个系统。

首先，系统应能够存储和管理顾客与餐厅信息，并允许轻松添加和删除数据。你需要选择合适的数据结构，并解释它们为什么适用。

接下来，系统必须实时处理订单，即按照订单接收的顺序处理订单。同时，顾客在确认订单之前应能够撤销最后添加的商品。你需要使用合适的数据结构设计该功能，并解释你的选择。

此外，系统应根据优先级分配配送员，例如最短配送时间或最近距离。你需要设计一种方法来高效选择最佳配送员，并说明你的方案如何优于简单搜索方法。

系统还必须找到餐厅与顾客之间的最短路线。你需要表示各个位置，并实现一种算法来确定最短路径，同时简要说明它的工作方式。

此外，系统应允许用户快速搜索食品项目，并以有组织的方式查看它们。你需要设计一种支持快速搜索和排序的结构，并解释其优势。

最后，系统应能够快速访问重要数据，例如用户资料和订单详情。

### 评分标准

| 组件 | 提出的数据结构技术 | 标准 | 分数 |
|---|---|---|---:|
| 用户与餐厅管理 | 数组 / 链表 | 正确选择数据结构，实现添加 / 删除 / 显示，并说明复杂度。 | 15 |
| 订单处理系统 | 队列 + 栈 | 正确使用 FIFO 队列和 LIFO 栈，实现可运行的订单流程及撤销功能，并给出理由。 | 15 |
| 配送分配 | 优先队列 / 最小堆 | 高效选择配送员，优先级逻辑正确，并与线性搜索进行性能比较。 | 15 |
| 路线优化 | 图 + Dijkstra 算法 | 正确表示图，实现最短路径，并解释算法和复杂度。 | 20 |
| 搜索与推荐 | BST / AVL 树 | 正确使用树结构，包含插入 / 搜索 / 遍历操作，并说明 `O(log n)` 效率。 | 15 |
| 数据检索优化 | 哈希表 / HashMap | 实现快速数据访问，使用键值对，并解释 `O(1)` 效率。 | 10 |
| 系统整合 | 所有数据结构结合 | 所有模块能够逻辑清晰、流畅地协同工作。 | 5 |
| 展示与报告 | 图示——栈、队列、图、树 | 解释清晰，报告结构合理，并包含相关图示。 | 5 |
| **总分** |  |  | **100** |
